capabilities: CONTEXT=SEARCH

ok search return (update) body body seen
* esearch (tag $searchtag)

ok store 1,2,5 +flags \seen
* esearch (tag $searchtag) addto ($pos 1:2,5)

ok store 1,2 -flags \seen
* esearch (tag $searchtag) removefrom ($pos2 1:2)

ok store 3:6 +flags \deleted
ok expunge
* esearch (tag $searchtag) removefrom ($pos3 $5)
* $3 expunge
* $4 expunge
* $5 expunge
* $6 expunge

ok cancelupdate "$searchtag"

ok store 1 +flags \seen
! esearch (tag $searchtag) addto ($pos4 1)
