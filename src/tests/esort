capabilities: ESORT

ok sort (arrival) us-ascii return (min) 1:5
* esearch (tag $tag) min 1
ok sort (arrival) us-ascii return (max) 1:5
* esearch (tag $tag) max 5
ok sort (arrival) us-ascii return (all) 1:5
* esearch (tag $tag) all 1:5
ok sort (arrival) us-ascii return (count) 1:5
* esearch (tag $tag) count 5
